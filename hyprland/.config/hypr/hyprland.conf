# let nwg-displays manage monitors
source = ~/.config/hypr/monitors.conf
source = ~/.config/hypr/workspaces.conf

# fallback for unkown monitors
monitor = , preferred, auto, 1

# key bindings
source = ~/.config/hypr/binds.conf

# cursors
env = HYPRCURSOR_THEME,catppuccin-mocha-dark-cursors
env = HYPRCURSOR_SIZE,23
env = XCURSOR_SIZE,23

# Some default env vars.
env = XDG_CURRENT_DESKTOP,Hyprland # Otherwise there is some "XDG_CURRENT_DESKTOP is managed externally ..." error
env = MOZ_ENABLE_WAYLAND,1
env = GDK_BACKEND,wayland
env = SSH_AUTH_SOCK,$XDG_RUNTIME_DIR/ssh-agent.socket

# for some these start as (inactive) when enabled, so start them manually here
exec-once = systemctl --user start hypridle.service
exec-once = systemctl --user start hyprpolkitagent.service

exec-once = hyprpm reload -nn

exec-once = swaybg -i $HOME/.config/hypr/current_wallpaper -m fill
exec-once = $HOME/Dotfiles/scripts/waybar-hotreload # status bar
exec-once = solaar -w hide -b solaar                # logitech devices
exec-once = fnott                                   # notifications
exec-once = syshud -p top                           # pretty OSD
exec-once = batsignal -w 15 -c 8                    # low battery notification
exec-once = blueman-applet                          # bluetooth tray icon

# clipboard history
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

input {
    kb_layout = de
    kb_variant = us
    kb_model =
    kb_options = ctrl:nocaps
    kb_rules =

    repeat_rate = 40
    repeat_delay = 300

    follow_mouse = 2
    float_switch_override_focus = 0

    touchpad {
        natural_scroll = false
        tap-to-click = false
    }

    sensitivity = 0
}

cursor {
    no_warps = true
    inactive_timeout = 5
}

general {
    gaps_in = 3
    gaps_out = 5
    border_size = 2
    col.active_border = rgba(eeeeff99)
    col.inactive_border = rgba(434343ff)

    layout = master

    allow_tearing = false
}

binds {
    # make movefocus move to another monitor when window is fullscreen
    movefocus_cycles_fullscreen = 0
    # workspace_back_and_forth = true
}

decoration {
    rounding = 2
    rounding_power = 4
    border_part_of_window = false

    blur {
        enabled = false
        size = 8
        noise = 0
        contrast= 1
    }

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1cee)
    }
}

animations {
    enabled = true

    # https://easings.net/#easeOutCubic
    bezier = easeOutCubic, 0.33, 1, 0.68, 1
    animation = global, 0
    # animation = windows, 1, 1.3, easeOutCubic
    # animation = windowsOut, 1, 1.3, easeOutCubic
    # animation = border, 1, 1.3, easeOutCubic
    # animation = borderangle, 1, 1.3, easeOutCubic
    # animation = fade, 1, 1.3, easeOutCubic
    animation = workspaces, 1, 1.3, easeOutCubic
}

master {
    orientation = "left"
    new_on_active = "after"
}

misc {
    background_color = 0x000000
    font_family = "SF Pro Text"
    enable_swallow = true
    swallow_regex = ^foot$
    disable_hyprland_logo = true
    vfr = true
}

debug {
  disable_logs = true
}

# this is from the example config so it has to be good
windowrule = suppressevent maximize, class:.*
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
