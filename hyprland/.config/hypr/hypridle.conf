general {
    # lock_cmd = gtklock -d
    # before_sleep_cmd = gtklock -d
    lock_cmd = pgrep hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
    # after_sleep_cmd = hyprctl dispatch dpms on
}
